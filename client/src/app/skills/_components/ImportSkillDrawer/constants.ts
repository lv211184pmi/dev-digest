/** Language filter pills for the Community tab — matches the static fixture
 *  list's `lang` values (server/src/modules/skills/import.ts). */
export const COMMUNITY_LANGUAGES = ["All languages", "TypeScript", "any"] as const;

export const DRAWER_WIDTH = 720;
